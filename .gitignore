# Datos y artefactos de runtime
# (cada árbol de dispositivo —jetson/, rubikpi/— tiene su propio data/)
data/
**/data/
**/_verify/
*.db
*.db-journal
*.mp4

# Config con secretos (usar config.example.yaml como plantilla)
# `config.yaml` sin barra ya cubre cualquier nivel: jetson/, rubikpi/, edge/
config.yaml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# OTA / pesos descargados
**/ota/

# Contador de flujo: data por-despliegue (corredor, contadores, evidencia)
**/Models/ContadorFlujo/zones_web/*.json
**/Models/ContadorFlujo/counters/
**/Models/ContadorFlujo/EVENTOS_CONTADOR/

# OS / IDE
.DS_Store
.idea/
.vscode/

# Pesos de modelos (binarios grandes; no versionar)
*.pt
*.onnx
*.engine
